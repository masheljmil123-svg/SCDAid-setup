services:
  - type: web
    name: scdaid-setup
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: OPENAI_MODEL
        value: gpt-4.1-mini
